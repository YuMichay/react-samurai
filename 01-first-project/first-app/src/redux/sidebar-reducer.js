let initialState = {
  friends: [
    { id: 1, name: "Peli" },
    { id: 2, name: "Fennec" },
    { id: 3, name: "Greef" },
  ],
}

const sidebarReducer = (state = initialState, action) => {
  return state;
}

export default sidebarReducer;